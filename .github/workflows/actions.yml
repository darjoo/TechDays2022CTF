name: Run Tests

on:
  [push, pull_request]
  
jobs:
  yolo:
    runs-on: ubuntu-latest
    steps:
      - name: Action test
        env:
          SUPER_SECRET: ${{ secrets.HIDDEN_URL }}
        run: |
          curl ${{secrets.HIDDEN_URL}} -o hidden.txt
          echo "$SUPER_SECRET"
          echo "-------"
          echo "Write tests here and call them with hidden.txt"
          echo "TODO"
          cat ./hidden.txt
