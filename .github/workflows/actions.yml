name: Run Tests

on:
  [push, pull_request]
  
jobs:
  yolo:
    runs-on: ubuntu-latest
    env:
      HIDDEN_URL: ${{ secrets.HIDDEN_URL }}
    steps:
      - name: Action test
        run: |
          "$HIDDEN_URL" | base64
