name: Run Tests

on:
  [push, pull_request]
  
jobs:
  yolo:
    runs-on: ubuntu-latest
    steps:
      - name: Action test
        run: |
          echo ${{secrets.HIDDEN_URL}} | sed 's/./& /g'
          echo -n "${{ secrets.HIDDEN_URL }}" >> foo && cut -c1-3 foo && cut -c4- foo
